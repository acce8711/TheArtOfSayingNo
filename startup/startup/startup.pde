import java.util.Iterator;

// Global variables
Graph graph = new Graph();
int cols, rows;
int tileSize;

// Path to follow
ArrayList<Edge> pathToFollow;

void setup() {
  
  pixelDensity(1);
  size(900,600);

}

void draw() {
  background(0);
}
